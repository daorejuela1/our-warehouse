<nav class="navbar navbar-expand-lg border-bottom ">
  <div class="container">
    <%= link_to root_path, class: "nav_logo" do %>
      <h1 class="navbar-brand display-4 font-weight-bold text-dark">OWH</h1>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="align-middle collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto navbar-right">
        <% if user_signed_in? %>
          <li>
            Hi, <%= link_to("@"+current_user.email.truncate(17), edit_user_registration_path, class: "navbar-text mr-3")%>
          </li>
          <%= render "shared/account_owner" if current_user.is_admin?%>
          <%= render "shared/common_user" %>
        <% else %>
          <%= render "shared/unsignup" %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
